# Future Work

- Add normalized DTOs for Neo4j nodes and relationships.
- Add a dedicated `agent_skill_outcomes` table for self-improvement analytics.
- Add a `token_usage_events` table for true 60-second runaway detection.
- Add Zod schemas for every request body and response payload.
- Add integration tests for BullMQ with a disposable Redis instance.
- Add OpenAI mocks for learning, prompt-injection, adversarial, and model-comparison routes.
- Add service-level RBAC for governance and security operations.
- Add observability metrics for job latency, queue failures, kill-switch changes, and learning-event volume.
